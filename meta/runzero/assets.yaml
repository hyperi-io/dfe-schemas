# Meta schema: runZero assets
#
# Assets: one row per device the console has scanned or imported, with its addresses, names, OS and hardware fingerprints, and per-asset counters.
#
# Columns are the union of keys across the rows of a live export, typed from
# the values observed; the record sits under `record`, which is where every
# expression reads from. Generated by scripts/generate_meta_schemas.py: a
# changed export becomes a new version entry, never an edit of a published one.

current: "1.0.0"
resource_type: core

versions:
  "1.0.0":
    date: "2026-09-13"
    type: model
    summary: "Initial runZero assets schema, from a live export"
    columns:
      - name: id
        type: uuid
        use_case: bloom
        expr: "@source: record.id"
        comment: "Row key of the export"
        _field_type: base

      - name: addresses
        type: string
        ch_override: "Array(String)"
        expr: "@source: record.addresses"
        comment: "List of strings; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: addresses_extra
        type: string
        ch_override: "Array(String)"
        expr: "@source: record.addresses_extra"
        comment: "List of strings; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: agent_external_ip
        type: string
        attribute: [nullable]
        expr: "@source: record.agent_external_ip"
        comment: "Null on every row observed, so the type is unknown"
        _field_type: base

      - name: agent_name
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.agent_name"
        _field_type: base

      - name: alive
        type: boolean
        use_case: dimension
        expr: "@source: record.alive"
        _field_type: base

      - name: attributes
        type: json
        attribute: [not_null]
        expr: "@source: record.attributes"
        comment: "{} when absent"
        _field_type: base

      - name: category
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.category"
        _field_type: base

      - name: comments
        type: string
        attribute: [nullable]
        expr: "@source: record.comments"
        comment: "Null on every row observed, so the type is unknown"
        _field_type: base

      - name: created_at
        type: integer
        use_case: range
        expr: "@source: record.created_at"
        comment: "Unix epoch seconds as exported"
        _field_type: base

      - name: credentials
        type: string
        attribute: [nullable]
        expr: "@source: record.credentials"
        comment: "Null on every row observed, so the type is unknown"
        _field_type: base

      - name: criticality
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.criticality"
        _field_type: base

      - name: criticality_rank
        type: integer
        use_case: range
        expr: "@source: record.criticality_rank"
        comment: "0 on every row observed"
        _field_type: base

      - name: custom_integration_ids
        type: string
        ch_override: "Array(String)"
        expr: "@source: record.custom_integration_ids"
        comment: "List, empty on every row observed; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: detected_by
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.detected_by"
        _field_type: base

      - name: domains
        type: string
        ch_override: "Array(String)"
        expr: "@source: record.domains"
        comment: "List of strings; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: eol_hw
        type: integer
        use_case: range
        expr: "@source: record.eol_hw"
        comment: "0 on every row observed"
        _field_type: base

      - name: eol_hw_eosale
        type: integer
        use_case: range
        expr: "@source: record.eol_hw_eosale"
        comment: "0 on every row observed"
        _field_type: base

      - name: eol_hw_ext
        type: integer
        use_case: range
        expr: "@source: record.eol_hw_ext"
        comment: "0 on every row observed"
        _field_type: base

      - name: eol_os
        type: integer
        use_case: range
        expr: "@source: record.eol_os"
        comment: "Unix epoch seconds as exported, 0 when unset"
        _field_type: base

      - name: eol_os_ext
        type: integer
        use_case: range
        expr: "@source: record.eol_os_ext"
        comment: "Unix epoch seconds as exported, 0 when unset"
        _field_type: base

      - name: first_seen
        type: integer
        use_case: range
        expr: "@source: record.first_seen"
        comment: "Unix epoch seconds as exported, 0 when unset"
        _field_type: base

      - name: first_task_created_at
        type: integer
        use_case: range
        expr: "@source: record.first_task_created_at"
        comment: "Unix epoch seconds as exported"
        _field_type: base

      - name: first_task_id
        type: uuid
        use_case: dimension
        expr: "@source: record.first_task_id"
        _field_type: base

      - name: foreign_attributes
        type: json
        attribute: [not_null]
        expr: "@source: record.foreign_attributes"
        comment: "{} when absent"
        _field_type: base

      - name: functions
        type: json
        attribute: [not_null]
        expr: "@source: record.functions"
        comment: "{} when absent"
        _field_type: base

      - name: has_geo
        type: boolean
        use_case: dimension
        expr: "@source: record.has_geo"
        _field_type: base

      - name: hosted_zone_name
        type: string
        attribute: [nullable]
        expr: "@source: record.hosted_zone_name"
        comment: "Null on every row observed, so the type is unknown"
        _field_type: base

      - name: hw
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.hw"
        _field_type: base

      - name: hw_product
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.hw_product"
        _field_type: base

      - name: hw_vendor
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.hw_vendor"
        _field_type: base

      - name: hw_version
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.hw_version"
        comment: "Empty string on every row observed"
        _field_type: base

      - name: last_agent_id
        type: uuid
        use_case: dimension
        expr: "@source: record.last_agent_id"
        _field_type: base

      - name: last_seen
        type: integer
        use_case: range
        expr: "@source: record.last_seen"
        comment: "Unix epoch seconds as exported"
        _field_type: base

      - name: last_task_id
        type: uuid
        use_case: dimension
        expr: "@source: record.last_task_id"
        _field_type: base

      - name: lowest_rtt
        type: integer
        use_case: range
        expr: "@source: record.lowest_rtt"
        _field_type: base

      - name: lowest_ttl
        type: integer
        use_case: range
        expr: "@source: record.lowest_ttl"
        comment: "0 on every row observed"
        _field_type: base

      - name: mac_vendors
        type: string
        ch_override: "Array(String)"
        expr: "@source: record.mac_vendors"
        comment: "List of strings; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: macs
        type: string
        ch_override: "Array(String)"
        expr: "@source: record.macs"
        comment: "List of strings; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: name
        type: string
        use_case: dimension
        expr: "@source: record.name"
        _field_type: base

      - name: names
        type: string
        ch_override: "Array(String)"
        expr: "@source: record.names"
        comment: "List of strings; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: newest_mac
        type: string
        use_case: dimension
        expr: "@source: record.newest_mac"
        _field_type: base

      - name: newest_mac_age
        type: string
        attribute: [nullable]
        expr: "@source: record.newest_mac_age"
        comment: "Null on every row observed, so the type is unknown"
        _field_type: base

      - name: newest_mac_vendor
        type: string
        attribute: [nullable]
        expr: "@source: record.newest_mac_vendor"
        comment: "Null on every row observed, so the type is unknown"
        _field_type: base

      - name: org_name
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.org_name"
        _field_type: base

      - name: organization_id
        type: uuid
        use_case: dimension
        expr: "@source: record.organization_id"
        _field_type: base

      - name: os
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.os"
        _field_type: base

      - name: os_product
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.os_product"
        _field_type: base

      - name: os_vendor
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.os_vendor"
        _field_type: base

      - name: os_version
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.os_version"
        _field_type: base

      - name: outlier_raw
        type: integer
        use_case: range
        expr: "@source: record.outlier_raw"
        _field_type: base

      - name: outlier_score
        type: integer
        use_case: range
        expr: "@source: record.outlier_score"
        _field_type: base

      - name: owners
        type: string
        ch_override: "Array(String)"
        expr: "@source: record.owners"
        comment: "List, empty on every row observed; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: ownership
        type: string
        ch_override: "Array(String)"
        expr: "@source: record.ownership"
        comment: "List, empty on every row observed; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: purdue_levels
        type: string
        attribute: [nullable]
        expr: "@source: record.purdue_levels"
        comment: "Null on every row observed, so the type is unknown"
        _field_type: base

      - name: risk
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.risk"
        _field_type: base

      - name: risk_rank
        type: integer
        use_case: range
        expr: "@source: record.risk_rank"
        _field_type: base

      - name: rtts
        type: json
        attribute: [not_null]
        expr: "@source: record.rtts"
        comment: "{} when absent"
        _field_type: base

      - name: scanned
        type: boolean
        use_case: dimension
        expr: "@source: record.scanned"
        _field_type: base

      - name: service_count
        type: integer
        use_case: range
        expr: "@source: record.service_count"
        _field_type: base

      - name: service_count_arp
        type: integer
        use_case: range
        expr: "@source: record.service_count_arp"
        _field_type: base

      - name: service_count_icmp
        type: integer
        use_case: range
        expr: "@source: record.service_count_icmp"
        _field_type: base

      - name: service_count_tcp
        type: integer
        use_case: range
        expr: "@source: record.service_count_tcp"
        _field_type: base

      - name: service_count_udp
        type: integer
        use_case: range
        expr: "@source: record.service_count_udp"
        _field_type: base

      - name: service_ports_tcp
        type: string
        ch_override: "Array(String)"
        expr: "@source: record.service_ports_tcp"
        comment: "List of strings; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: service_ports_udp
        type: string
        ch_override: "Array(String)"
        expr: "@source: record.service_ports_udp"
        comment: "List of strings; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: service_products
        type: string
        ch_override: "Array(String)"
        expr: "@source: record.service_products"
        comment: "List of strings; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: service_protocols
        type: string
        ch_override: "Array(String)"
        expr: "@source: record.service_protocols"
        comment: "List of strings; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: services
        type: json
        attribute: [not_null]
        expr: "@source: record.services"
        comment: "{} when absent"
        _field_type: base

      - name: site_id
        type: uuid
        use_case: dimension
        expr: "@source: record.site_id"
        _field_type: base

      - name: site_name
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.site_name"
        _field_type: base

      - name: software_count
        type: integer
        use_case: range
        expr: "@source: record.software_count"
        _field_type: base

      - name: source_ids
        type: integer
        ch_override: "Array(Int64)"
        expr: "@source: record.source_ids"
        comment: "List of integers; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: sources
        type: string
        ch_override: "Array(String)"
        expr: "@source: record.sources"
        comment: "List of strings; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: subnets
        type: string
        attribute: [nullable]
        expr: "@source: record.subnets"
        comment: "Null on every row observed, so the type is unknown"
        _field_type: base

      - name: surface_types
        type: integer
        ch_override: "Array(Int64)"
        expr: "@source: record.surface_types"
        comment: "List of integers; [] when absent"
        codec: "ZSTD(1)"
        _field_type: base

      - name: tag_descriptions
        type: string
        attribute: [nullable]
        expr: "@source: record.tag_descriptions"
        comment: "Null on every row observed, so the type is unknown"
        _field_type: base

      - name: tags
        type: json
        attribute: [not_null]
        expr: "@source: record.tags"
        comment: "{} when absent"
        _field_type: base

      - name: type
        type: string
        attribute: [lowcardinality]
        use_case: dimension
        expr: "@source: record.type"
        _field_type: base

      - name: updated_at
        type: integer
        use_case: range
        expr: "@source: record.updated_at"
        comment: "Unix epoch seconds as exported"
        _field_type: base

      - name: vulnerability_count
        type: integer
        use_case: range
        expr: "@source: record.vulnerability_count"
        _field_type: base
