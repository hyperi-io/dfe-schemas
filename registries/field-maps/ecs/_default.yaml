resource_type: core
standard: ecs
version: "8.11"
description: Default ECS (Elastic Common Schema) field mappings for DFE platform
mappings:
  source.ip: source_ip
  destination.ip: destination_ip
  source.port: source_port
  destination.port: destination_port
  user.name: user_name
  host.name: hostname
  process.name: process_name
  process.command_line: command_line
  process.pid: process_id
  process.parent.name: parent_process_name
  process.parent.command_line: parent_command_line
  process.parent.pid: parent_process_id
  event.category: event_category
  event.action: event_action
  event.outcome: event_outcome
  file.name: target_filename
  file.path: file_path
  registry.key: registry_key
  registry.value: registry_value
  "@timestamp": _timestamp
