# Source: main
#
# Where the loader puts a record it has nowhere else to send

resource_type: core
description: Where the loader puts a record it has nowhere else to send
state: active
deployed_version: "1.0.0"
current: "1.0.0"

versions:
  "1.0.0":
    date_time: "2026-09-11"
    header:
      type: timeseries
      version: "1.0.0"
    schema: {}
